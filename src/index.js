import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';          // Se hai uno stile CSS globale
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
